(() => {
  'use strict';
  const body = document.body;
  const csrfToken = document.querySelector('meta[name="csrf-token"]')?.content || '';
  const focusableSelector = 'a[href],button:not([disabled]),input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex="-1"])';
  const ensureMainTarget = () => { const main=document.querySelector('main');if(main&&!main.id)main.id='main-content';if(main&&!main.hasAttribute('tabindex'))main.setAttribute('tabindex','-1'); };
  const ensureLiveRegion = () => { let live=document.querySelector('[data-labs-live-region]');if(live)return live;live=document.createElement('div');live.className='labs-sr-only';live.setAttribute('aria-live','polite');live.setAttribute('aria-atomic','true');live.dataset.labsLiveRegion='';body.append(live);return live; };
  const securePostForms=()=>{if(!csrfToken)return;document.querySelectorAll('form').forEach((form)=>{const method=(form.getAttribute('method')||'get').toLowerCase();if(method!=='post'||form.querySelector('input[name="_csrf"]'))return;const input=document.createElement('input');input.type='hidden';input.name='_csrf';input.value=csrfToken;form.prepend(input);});};
  const secureFetch=()=>{if(!window.fetch||!csrfToken||window.__trainingLabFetchSecured)return;const nativeFetch=window.fetch.bind(window);window.fetch=(input,init={})=>{const requestUrl=typeof input==='string'?input:input?.url;const url=new URL(requestUrl||window.location.href,window.location.href);const method=String(init.method||(input instanceof Request?input.method:'GET')).toUpperCase();const next={...init,credentials:init.credentials||'same-origin'};if(url.origin===window.location.origin&&!['GET','HEAD','OPTIONS'].includes(method)){const headers=new Headers(input instanceof Request?input.headers:undefined);new Headers(init.headers||{}).forEach((value,key)=>headers.set(key,value));headers.set('X-CSRF-Token',csrfToken);next.headers=headers;}return nativeFetch(input,next);};window.__trainingLabFetchSecured=true;};
  const announce=(message)=>{const live=ensureLiveRegion();if(!message)return;live.textContent='';window.setTimeout(()=>{live.textContent=message;},20);};
  const trapFocus=(event,container)=>{if(event.key!=='Tab'||!container)return;const items=[...container.querySelectorAll(focusableSelector)].filter((item)=>item.offsetParent!==null);if(!items.length)return;const first=items[0],last=items[items.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus();}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus();}};
  const setupDrawer=({openButton,drawer,openClass,closeSelector,label})=>{if(!openButton||!drawer)return()=>{};let returnFocus=openButton;const setState=(open)=>{body.classList.toggle(openClass,open);openButton.setAttribute('aria-expanded',String(open));drawer.setAttribute('aria-hidden',String(!open));if(open){returnFocus=document.activeElement instanceof HTMLElement?document.activeElement:openButton;drawer.querySelector(focusableSelector)?.focus();announce(`${label} opened`);}else{returnFocus?.focus?.();announce(`${label} closed`);}};openButton.addEventListener('click',()=>setState(true));document.querySelectorAll(closeSelector).forEach((node)=>node.addEventListener('click',()=>setState(false)));drawer.addEventListener('keydown',(event)=>trapFocus(event,drawer));drawer.setAttribute('aria-hidden','true');return()=>setState(false);};
  ensureMainTarget();ensureLiveRegion();securePostForms();secureFetch();
  document.querySelectorAll('[data-labs-toggle]').forEach((toggle)=>toggle.addEventListener('click',()=>{const selector=toggle.getAttribute('data-labs-toggle');const target=selector?document.querySelector(selector):null;if(!target)return;const open=target.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(open));}));
  const closeMenu=setupDrawer({openButton:document.querySelector('[data-labs-menu-open]'),drawer:document.querySelector('#labs-primary-nav'),openClass:'labs-nav-open',closeSelector:'[data-labs-menu-close]',label:'Main menu'});
  const closeWorkspace=setupDrawer({openButton:document.querySelector('[data-labs-workspace-open]'),drawer:document.querySelector('#labs-workspace-nav'),openClass:'labs-workspace-open',closeSelector:'[data-labs-workspace-close]',label:'Training menu'});
  document.addEventListener('keydown',(event)=>{if(event.key==='Escape'){closeMenu();closeWorkspace();}});
  const errorSummary=document.querySelector('[data-labs-error-summary]');if(errorSummary)requestAnimationFrame(()=>errorSummary.focus());
  document.querySelectorAll('[role="status"],[role="alert"]').forEach((node)=>{if(!node.hasAttribute('aria-live'))node.setAttribute('aria-live',node.getAttribute('role')==='alert'?'assertive':'polite');});
})();
